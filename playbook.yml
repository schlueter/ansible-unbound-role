---
- hosts: unbound
  become: yes
  gather_facts: yes
  vars:
    unbound_local_data:
      -
        name: example.com
        type: A
        class: IN
        ttl: 300
        ip_address: 127.0.1.42
        ptr: yes
  roles:
    - "{{ playbook_dir  }}"
